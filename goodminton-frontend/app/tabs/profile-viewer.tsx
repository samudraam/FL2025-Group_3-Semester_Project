import ProfileViewer from '../../components/ProfileViewer';

/**
 * Profile viewer screen that displays user profile information
 * This is a wrapper component that uses the ProfileViewer component
 */
export default ProfileViewer;
